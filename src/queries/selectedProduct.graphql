query SelectedProduct {
    selectedProduct @client {
        id,
        title,
        stock,
        unit,
        category {
            id,
            title
        },
        cover {
            path
        }
    }
}